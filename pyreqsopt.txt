# Optional Python Modules for PyExPool (pyexpool / mpepool)
# Jobs auto-balancing on execution
psutil>=5.2
# Future utilities for Python2/3 compatibility
future>=0.16
# Enum class for Python2 to be compatible with Python3
enum34>=1.1
# Web UI
bottle>=0.12
#flask>=1.0.1
# Unit tests (only for Python2)
mock>=2.0
